name: build

on:
  push:
    tags-ignore:
      - '*'
    branches:
      - '**'

env:
  REGISTRY: ghcr.io
  POSTGRES_USER: test-bruker
  POSTGRES_PASSWORD: 1234567
  POSTGRES_DB: test-db

jobs:
  build:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3

      -
        name: Log in to GHCR
        uses: docker/login-action@v1.12.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.GITHUB_TOKEN }}
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: |
            ghcr.io/${{ github.repository }}

      -
        name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - 
        name: Deploy
        run: |
          docker run -dp 5000:5000 ikt206-flask-example_application
          

  # test:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Run with setup-python 3.9
  #       uses: actions/setup-python@v3
  #       with:
  #         python-version: 3.9
  #     - name: Install  requirements
  #       run: |
  #         pip install -r src/requirements.txt
  #     - name: Run tests
  #       run: coverage run src/app_test.py
  #     - name: Test report
  #       run: coverage report

  # deploy:
  #   needs: build
  #   permissions:
  #     contents: read
  #     packages: write
  #   runs-on: self-hosted
  #   steps:
  #     - name: Something
  #       run: |
  #         ssh ${{ secrets.VM }} "docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.GITHUB_TOKEN }} ${{ env.REGISTRY }} && docker-compose pull && docker-compose up -d"
  #         docker-compose -f docker-compose.yml up
      # -
      #   name: Checkout
      #   uses: actions/checkout@v3
      # -
      #   name: Create release
      #   id: create-release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     tag_name: ${{ github.ref }}
      #     release_name: Release ${{ github.ref }}
      # -
      #   name: Uplode release asset
      #   uses: actions/upload-release-asset@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     upload_url: ${{ steps.create-release.outputs.upload_url }}


